<h1>Editing game</h1>

 <div class="edit-game">
   <a href="#help">Help?</a>
   <%= render 'form' %>
</div>
<div class="display-game">
  <%= link_to "Open Game in Another Window", play_path(@game.id), :target => "_blank"   %> <br/>
  <iframe width="640" height="480" src="<%= play_path(@game.id) %>">
  </iframe>
  
</div>

<%= link_to 'Show', @game %> |
<%= link_to 'Back', games_path %>

<hr/>
<p>Please only delete your own game</p>
<%= link_to 'Destroy', @game, :confirm => 'Really? you really sure about this?', :method => :delete %></td>

<%= render 'help' %>
