<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Put yer code here:" %><br />
    <%= f.text_area :script, :cols => 80, :rows => 30, %>
  </div>
  <div class="field">
    <%= f.label :name, "Name of Your Game:" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :author, "Who are You?" %><br />
    <%= f.text_field :author %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
